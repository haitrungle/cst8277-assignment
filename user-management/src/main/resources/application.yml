spring:
  datasource:
    url: 'jdbc:mysql://${MYSQL_HOST:localhost}:3306/db?createDatabaseIfNotExist=TRUE'
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: true
    show-sql: true
  sql:
    init:
      mode: always
  
  security:
    oauth2:
      client:
        registration:
          github:
            clientId: 643f05eece3d9fbc0d3a
            clientSecret: 2dad7e84b7ff92a31ded71bfad0ac9a872143e68